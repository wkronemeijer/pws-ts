/// <reference path="./../common.ts"/>

module TSP {
    "use strict"
    
    OptHeuristics.push({
        name: "2-Opt",
        solve(vertices: Vector[]): Vector[] {
            
            
            /// let's make the magic happen
            
            
            
            
            return vertices
        }
    })
    
    
}