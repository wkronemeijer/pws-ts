/// <reference path="./../common.ts"/>

module TSP {
    "use strict"
    
    
    Heuristics.push({
        name: "Grootste Hoek",
        solve(vertices: Vector[]): Vector[] {
            
            // stub
            
            return vertices
        }
    })
}
